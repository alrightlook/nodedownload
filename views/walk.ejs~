<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
	<% items.forEach(function(item) { %>
		<li><%= item%></li>
	<% })%>
  </body>
</html>
